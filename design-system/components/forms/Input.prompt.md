A text field — sunken warm well, optional label and leading icon, soft indigo focus ring.

```jsx
<Input label="Guild name" value={v} onChange={e=>set(e.target.value)} placeholder="The Night Owls" />
<Input icon={<Mail size={18} />} placeholder="friend@email.com" type="email" />
```

Pass `onNight` for dark reader surfaces.
