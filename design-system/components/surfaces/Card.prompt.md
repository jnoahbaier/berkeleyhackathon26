Talegate's base surface — a soft, big-radius card with warm low-opacity elevation. Wrap almost any content block in it.

```jsx
<Card elevation="md" pad={24}>…</Card>
<Card interactive onClick={open}>Tap me</Card>
<Card onNight>Reader panel</Card>
```

`elevation` flat→lg; `interactive` adds a gentle hover lift; `onNight` switches to dark reader styling.
