The app's modal bottom sheet — large top corners and a grab handle. Use for choices, story details, settings.

```jsx
<Sheet title="Tonight's choice">…</Sheet>
<Sheet title="The Ember Gate" onNight>…</Sheet>
```

Pass `onNight` when it sits over the dark reader.
